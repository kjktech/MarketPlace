{# extends "account.master" % #}
{% extends "layouts.app_master_dashboard" %}

{% block content %}
<style>
    .panel-table .panel-body .table-bordered{
        border-style: none;
        margin:0;
        width: 680px;
    }
    .panel-table .panel-body .table-bordered > thead > tr > th:last-of-type {
        text-align:center;
    }
    .fa-pencil {
      color: white;
    }
</style>
<section class="user-profile">
   <!--
    <div class="card panel-default">
        <h6 class="card-header bg-white"><i class="mdi mdi-account mr-2"></i> {{ __('Production') }}</h6>
        <a href="{{url('post')}}?store={{store}}" class="btn btn-link btn-xs"><i class="mdi mdi-plus"></i> Add new</a>
        <div class="card-body">


            <table class="table table-sm border-top-0">
                <thead class="thead-light">
                <tr>
                    <th scope="col">{{ __('Title') }}</th>
                    <th scope="col" class="d-none d-sm-block">{{ __('Date Created') }}</th>
                    <th scope="col"></th>
                </tr>
                </thead>
                <tbody>
                {% for i, item in listings %}
                    <tr>
                        <td><a href="{{ item.url }}" class="text-dark">{{ item.title | str_limit(40) }}</a></td>
                        <td class="d-none d-sm-block">{{ item.created_at.toFormattedDateString() }}</td>
                        <td class="text-right"><a href="{{ item.edit_url }}">{{ __('edit') }}&nbsp;<i class="mdi mdi-chevron-double-right"></i></a></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            </div>
            </div>
            -->
            <div class="panel panel-default panel-table">
                <div class="panel-heading">
                    <div class="row">
                        <div class="col col-md-11">
                            <h2 class="panel-title">{{ __('Products') }}</h2>
                        </div>
                        <div class="col col-md-1">
                            <a href="{{url('post')}}?store={{store}}" class="btn btn-sm btn-danger btn-create">Create New</a>
                        </div>
                    </div>
                </div>
                <br>
                <div class="panel-body">
                    <table class="table table-striped table-bordered table-list">
                        <thead>
                        <tr>
                            <th class="hidden-xs">{{ __('Title') }}</th>
                            <th>{{ __('Date Created') }}</th>
                            <th>{{ __('Activated On') }}</th>
                            <th><em class="fa fa-cog"></em></th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for i, item in listings %}
                            <tr>
                                <td><a href="{{ item.url }}" class="text-dark">{{ item.title | str_limit(40) }}</a></td>
                                <td class="hidden-xs">{{ item.created_at.toFormattedDateString() }}</td>
                                <td class="hidden-xs">{{ item.is_admin_verified }}</td>
                                <td align="center">
                                    <a href="{{ item.edit_url }}" class="btn btn-danger"><em class="fa fa-pencil"></em></a>
                                </td>
                            </tr>
                        {% endfor %}
                        {{ stores.links() | raw }}
                        </tbody>
                    </table>
                </div>
            </div>
            {{ listings.links() | raw }}


</section>
{% endblock %}
